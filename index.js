const colours = ["#C5E5A5"];
/**
 * TODO:
 * Change all highlights to a single highlight class and then use that to get all elements
 * that require a different colour and change it randomly using the given colors already besides
 * yellow, which is default.
 * 
 * ADD MORE CERTIFICATES
 */